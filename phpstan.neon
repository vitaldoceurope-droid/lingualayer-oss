includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5
    paths:
        - src
    # The package boots inside a host Laravel app; these are resolved there.
    ignoreErrors:
        - '#Unsafe usage of new static#'
    reportUnmatchedIgnoredErrors: false
